{
  "name": "deep-azure-sample",
  "version": "1.0.0",
  "private": true,
  "description": "JSON sample - keys, strings, numbers and booleans should each be distinct.",
  "type": "module",
  "sideEffects": false,
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "dev": "vite --port 5173",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
